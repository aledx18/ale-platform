// Punto de entrada público del paquete.
// Todo lo que se exporte acá es lo que van a poder importar
// tanto la app playground (local) como cualquier otro repo externo.

export function ping(): string {
  return "pong";
}
